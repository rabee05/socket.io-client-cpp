{
    "buildType": {
        "default": "debug",
        "description": "The build type",
        "choices": {
            "debug": {
                "short": "Debug",
                "long": "Debug build - HTTP/WS only (no TLS)",
                "buildType": "Debug",
                "settings": {
                    "CMAKE_BUILD_TYPE": "Debug"
                }
            },
            "release": {
                "short": "Release",
                "long": "Release build - HTTPS/WSS with TLS",
                "buildType": "Release",
                "settings": {
                    "CMAKE_BUILD_TYPE": "Release"
                }
            }
        }
    }
}
